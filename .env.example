WEBHOOK=https://example.com/webhook
PORT=3000
# RateLimitWindow defines the time window to apply the limit of requests per IP.
# RateLimitMax defines the maximum number of requests allowed per IP in that window.
# Example: 100 requests per 15 minutes.
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_MAX=50
